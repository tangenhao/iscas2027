% !TEX root = ../main.tex

\section{Evaluation}
\label{sec:eval}

\subsection{Experimental Setup}
\label{subsec:setup}

\subsubsection{Algorithm Setup}
We evaluate Open-Sora v1.2 STDiT3-XL/2~\cite{zheng2024opensora} on
VBench~\cite{huang2024vbench}. All methods generate 64-frame,
$512\times512$ videos with 30 rectified-flow steps under identical
prompts and sampling settings. We compare Original (FP16),
ViDiT-Q~\cite{zhao2025viditq} (linear-only W8A8),
TeaCache~\cite{liu2025teacache} (FP16 timestep caching), and Ours
(QAC with W8A8 quantization for both linear and attention operators).
We report VBench together with representative generated frames for
quantitative and qualitative quality evaluation.

\subsubsection{Hardware Setup}
We compare against ViDA~\cite{ding2025vida} as the custom-accelerator
baseline and NVIDIA A100~\cite{nvidiaA100} as the GPU baseline.
The complete Q-DARE design, including logic and on-chip SRAM, is
synthesized in TSMC 28\,nm at 1\,GHz. Following
ViDA~\cite{ding2025vida}, we normalize the synthesized 28-nm PPA to
7\,nm using DeepScaleTool~\cite{sarangi2021deepscaletool}.
A cycle-level simulator models module execution and data dependencies
to obtain accelerator latency. For a fair comparison with ViDA, we
adopt the same 1\,GHz frequency and 256\,GB/s off-chip memory
bandwidth. The A100 is evaluated directly on the same Open-Sora
workload without technology normalization.

\subsection{Algorithm Quality}
\label{subsec:quality}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig/gen_img.pdf}
  \caption{Algorithm-level quality comparison on Open-Sora v1.2.
  VBench scores and representative frames are reported under the same
  prompts and sampling settings.}
  \label{fig:quality}
\end{figure}

Fig.~\ref{fig:quality} compares the original FP16 model with
ViDiT-Q, TeaCache, and our QAC-based inference. Ours achieves a VBench
score of 76.61, close to the original model (76.77), while combining
timestep caching with W8A8 execution for both linear and attention
operators. The representative frames show that the generated visual
content is well preserved under the proposed cache--quantization
co-design.

\subsection{Hardware Evaluation}
\label{subsec:hw_eval}

\subsubsection{Comparison with ViDA}
Table~\ref{tab:vida_comparison} compares Q-DARE with
ViDA~\cite{ding2025vida} after technology normalization to 7\,nm.
Area efficiency is defined as end-to-end denoising throughput per unit
area. Our native 28-nm synthesis yields 4.2624\,mm$^2$ area and
116.372\,mW power at 1\,GHz.

\begin{table}[t]
\centering
\caption{Hardware comparison with ViDA after 7-nm normalization.}
\label{tab:vida_comparison}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{1.05}

\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}ccc@{}}
\toprule
\textbf{Metric} &
\textbf{Q-DARE (Ours)} &
\textbf{ViDA~\cite{ding2025vida}} \\
\midrule

Technology
  & 28$\rightarrow$7 nm
  & 32$\rightarrow$7 nm \\

Frequency
  & 1.0\,GHz
  & 1.0\,GHz \\

Precision
  & INT8/FP16
  & FP16 \\

\multirow{2}{*}{Compute}
  & 1296 INT8 MACs
  & \multirow{2}{*}{640 FP16 MACs} \\
  & 36 FP16 MACs
  & \\

On-chip SRAM
  & 238.5\,KB
  & N/R \\

Memory BW
  & 256\,GB/s
  & 256\,GB/s \\

Area (mm$^2$)
  & 0.134
  & 2.03 \\

Power (mW)
  & 43.6
  & N/R \\

Throughput (steps/s)
  & \textbf{[TBD]}
  & \textbf{[TBD]} \\

Area Eff. (steps/s/mm$^2$)
  & \textbf{[TBD]}
  & \textbf{[TBD]} \\

\bottomrule
\end{tabular}%
}
\end{table}

\subsubsection{Comparison with GPU}
We further compare Q-DARE with NVIDIA A100 on the same Open-Sora
workload. Unlike the technology-normalized custom-accelerator
comparison, the A100 results are measured directly on the GPU.
Fig.~\ref{fig:gpu_comparison} reports end-to-end denoising throughput
and energy efficiency in steps/s and steps/s/W, respectively.

\begin{figure}[t]
  \centering
  \IfFileExists{fig/gpu_comparison.pdf}
    {\includegraphics[width=\columnwidth]{fig/gpu_comparison.pdf}}
    {\fbox{\parbox[c][1.05in][c]{0.94\columnwidth}{\centering\footnotesize
    (a) Throughput (steps/s) \qquad
    (b) Energy efficiency (steps/s/W)}}}
  \caption{End-to-end comparison with NVIDIA A100 on the same
  Open-Sora workload: (a) denoising throughput and (b) energy
  efficiency. Higher is better.}
  \label{fig:gpu_comparison}
\end{figure}
