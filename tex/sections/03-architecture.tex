% !TEX root = ../main.tex
\section{DARE: Dimension-Adaptive Reconfigurable Engine}
\label{sec:hardware}

As illustrated in the lower path of Fig.~\ref{fig:overview}, factorized spatial/temporal attention exposes a \emph{matrix-axis mismatch}: the variable sequence dimension $L$ maps to the output axis of $QK^{\mathsf T}$ but to the reduction axis of $PV$. For the default \ModelName{} workload, $L$ alternates between $S=\SpatialSeq{}$ and $T=\TemporalSeq{}$. Under CFG, this yields \SpatialContexts{} spatial contexts and \TemporalContexts{} temporal contexts, i.e., temporal attention exposes $27\times$ more independent contexts that are $27\times$ shorter. DARE therefore reconfigures the same $36\times36$ INT8 PEA along these two axes rather than provisioning separate datapaths.

The baseline PEA contains 36 output lanes, each with a 36-product INT8 dot product. Channel dimensions are naturally aligned: $d=72=2\times36$ and the STDiT hidden dimension is $1152=32\times36$. Hence the dominant inefficiency comes from the variable spatial/temporal token dimension rather than from the channel dimension.

\begin{figure}[t]
  \centering
  \placeholderfig{0.95in}{
  \textbf{Fig. 2 placeholder: dual-axis adaptation in DARE.}\\[1pt]
  (a) $QK^{\mathsf T}$: baseline 36 lanes/head $\rightarrow$ four 9-lane head groups.\\
  (b) $PV$: padded variable-length reductions $\rightarrow$ continuous product stream with segment tags and boundary-aware reduction.}
  \caption{DARE adapts the same PEA along the output axis for $QK^{\mathsf T}$ and the reduction axis for $PV$.}
  \label{fig:dare_detail}
\end{figure}

\subsection{Output-Axis Adaptation for $QK^{\mathsf T}$}

A conventional mapping assigns all 36 output lanes to one attention head. For temporal attention, only 15 lanes carry useful scores, giving $15/36=\BaseTemporalQKUtil{}$ utilization. DARE partitions the array into four independent 9-lane head groups:
\begin{equation}
36\ \text{lanes} \rightarrow 4\ \text{heads}\times 9\ \text{lanes/head}.
\end{equation}
For $T=15$, each head completes in two 9-lane groups, increasing temporal utilization to $15/18=\OursTemporalQKUtil{}$. For $S=405$, $405/9=45$ exactly, eliminating the spatial tail and reaching \OursSpatialQKUtil{}.

\subsection{Boundary-Aware Segmented Reduction for $PV$}

In $PV$, the same sequence length $L$ becomes the reduction dimension. Padding each short reduction to 36 products wastes multipliers. DARE instead treats successive dot products as a continuous product stream. Each 36-product beat carries reduction-boundary metadata; every reduction-tree node propagates a segment tag and only accumulates neighboring values with identical tags. Completed segments are committed immediately, while an unfinished tail is carried into the next beat. This converts padding bubbles into useful products from adjacent reductions without mixing independent outputs.

% IMPLEMENTATION CHECK BEFORE FINAL SUBMISSION:
% The current segmented temporal PV RTL must support/verify the default T=15
% path before claiming default-workload PV speedup in the paper.

To feed both execution modes without explicit tensor reshaping, the load unit uses stride-programmable address generation for the logical $[T,S,C]$ and $[S,T,C]$ views. This supporting mechanism changes only address traversal and does not require an additional activation-reshaping buffer.
